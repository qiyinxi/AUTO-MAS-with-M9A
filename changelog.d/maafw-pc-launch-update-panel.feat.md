MFW 脚本编辑页整改：PC 游戏默认改为由 MAS 启动并在结束后关闭（另一项改名「使用其他方式启停游戏」，去掉多余的关闭开关），新增「尝试修改 Unity 类游戏分辨率」（1920×1080 / 1280×720）可在启动前临时改注册表并在关闭后恢复；基础信息区改为左侧 interface 概览表、右侧运行环境日志，「读取 interface」按钮改为「准备运行环境」；项目更新区左侧保留版本与 GitHub、右侧为更新过程日志（下载进度与速度、全量或增量、逐文件覆盖进度），两处结论都直接在日志末行标色，不再单独弹提示；Mirror 酱 CDK 自动填入 MAS 更新设置里的值，手动更新按钮挪到 CDK 旁边；ADB 控制策略表精简为截图 / 输入两行并支持按 Emulator 2.0 所选设备解析；去掉一批只是复述标签的问号说明与提示条，深色模式下不再有成块的底色；用户页的任务队列与任务配置两栏改为等高定高、各自内部滚动；新建 MFW 脚本的单次运行时间限制默认值改为 120 分钟
